# ZOMURUD v3
Admin + Orders + PDF invoices.
Run: npm install && npm start
Store: /
Admin: /admin
Invoice: /api/invoice/:id
Payment and shipping intentionally deferred.
Before public commerce: add real admin authentication, persistent database/storage, backups and HTTPS.
